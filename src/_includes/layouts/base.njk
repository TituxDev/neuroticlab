<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{% block title %}NeuroticLab{% endblock %}</title>
        <style>
/* === CSS ORGANIZADO DE neuroticlab === */

/* === VARIABLES === */
:root {
    --border-format: 1px solid white;
    --frame-bgcolor: #3b5bdb;
    --menu-cursor: pointer;
}

/* === RESET / BASE === */
body {
    overflow: hidden;
    font-family: 'Fire Code', monospace;
    margin: 0;
    padding: 0;
}

/* === LAYOUT === */
.site-layout {
    display: flex;
    flex-direction: column;
    height: 100vh;
    width: 100vw;
}

.horizontal-bar {
    display: flex;
    width: 100%;
    height: auto;
}

.container {
    display: flex;
}

/* === UTILIDADES === */
.vertical-division > * {
    border-right: var(--divider-format, 1px solid black);
}
.vertical-division > *:last-child {
    border-right: none;
}

.horizontal-division > * {
    border-bottom: var(--divider-format, 1px solid black);
}
.horizontal-division > *:last-child {
    border-bottom: none;
}

.drop-menu {
    cursor: var(--menu-cursor);
}
.menu-content {
    display: none;
}
.drop-menu:hover .menu-content {
    display: block;
}

/* === HEADER === */
.site-header {
    min-height: 50px;
    background-color: var(--frame-bgcolor);
}

.site-header .header-nav {
    display: flex;
    width: 100%;
}

.site-header .header-nav.vertical-division {
    --divider-format: var(--border-format);
}

.site-header .header-nav .logo {
    display: flex;
    flex-shrink: 0;
    width: 150px;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    font-weight: bold;
    color: #ffb86c;
}

.site-header .header-nav .main-nav {
    flex: 1 1 auto;
    min-width: 150px;
    display: flex;
    position: relative;
    overflow-x: auto;
    overflow-y: visible;
    max-height: 100%;
}

.site-header .header-nav .main-nav > * {
    display: flex;
    flex-shrink: 0;
    width: 150px;
    align-items: center;
    justify-content: center;
    border-right: var(--border-format);
    text-decoration: none;
    font-weight: bold;
    color: #00ffff;
}

.site-header .header-nav .support {
    flex-shrink: 0;
    position: relative;
    margin-left: auto;
}

.site-header .header-nav .support > span {
    display: flex;
    height: 100%;
    padding: 0 15px;
    align-items: center;
    font-weight: bold;
    color: #ffd86c;
}

.site-header .header-nav .support > .menu-content {
    position: absolute;
    z-index: 1000;
    top: 100%;
    right: 0;
    min-width: 160px;
    padding: 10px;
    background-color: #444;
    border: 1px solid #666;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
}

.site-header .header-nav .support > .menu-content > * {
    padding: 5px 0;
    line-height: 1.6;
    text-decoration: none;
    color: white;
}

/* === MAIN (CONTENIDO PRINCIPAL) === */
main.content {
    flex: 1 1 auto;
    overflow-y: auto;
    background-color: #2c313a;
    display: flex;
    color: #dcdcdc;
    padding-left: clamp(0px, calc((100vw - 300px - 906px) / 2), 200px);
}

main.content .inner {
    display: flex;
    flex-direction: column;
    max-width: 906px;
    margin: auto 0;
    padding: 20px;
}

main.content .inner * {
    margin: 0;
}

main.content .inner p {
    width: 100%;
    margin-top: 0;
    margin-bottom: 15px;
    text-align: justify;
    line-height: 1.3;
}

main.content .inner h1 {
    display: flex;
    justify-content: center;
    font-size: 1.5em;
    text-decoration: none;
    width: 100%;
    color: rgb(255, 83, 255);
}

main.content .inner h2,
main.content .inner h3,
main.content .inner h4,
main.content .inner h5,
main.content .inner h6 {
    margin-top: 15px;
    margin-bottom: 8px;
    color: rgb(62, 208, 67);
    text-decoration: underline;
    font-size: 1.2em;
}

main.content .inner > *:first-child {
    margin-top: 0;
}

main.content .inner > *:last-child {
    margin-bottom: 0;
}

/* === ASIDE / MODULOS === */
.site-middle {
    display: flex;
    flex: 1 1 auto;
    overflow: hidden;
}

.site-middle .modules {
    display: flex;
    flex-direction: column;
    border-right: var(--border-format);
    width: 301px;
    min-width: 301px;
    height: 100%;
    background-color: #1f2229;
    color: #00ffff;
}

.site-middle .modules span {
    border-bottom: var(--border-format);
    padding: 15px 40px;
    font-weight: bold;
    text-align: left;
    font-size: 1.1rem;
    letter-spacing: 2px;
    background-color: #191b20;
    color: #ffd86c;
}

.site-middle .modules ol {
    flex: 1 1 auto;
    overflow-y: scroll;
    width: 100%;
    list-style-position: inside;
    margin: 0;
    padding: 0;
    scrollbar-width: thin;
}

.site-middle .modules ol > * {
    margin: 10px 20px;
    text-align: left;
    color: #ffd86c;
}

.site-middle .modules ol a {
    text-decoration: none;
    color: #00ffff;
}

.site-middle .modules ol a:hover {
    font-size: 1.1rem;
}

.site-middle .modules ol a:visited {
    color: #c080ff;
}

/* === FOOTER === */
.site-footer {
    background-color: var(--frame-bgcolor);
}

.site-footer * {
    color: #dddddd;
    flex-shrink: 0;
}

footer.site-footer .container {
    width: 100%;
    height: 35px;
    justify-content: space-around;
}

footer.site-footer .container p {
    margin: 0;
    margin-top: 5px;
}        </style>
    </head>

<!-- BODY -->
    <body class="site-layout">
        <header class="horizontal-bar site-header">
            <nav class="header-nav vertical-division">
                <a href="/" class="logo">NeuroTIClab</a>
                <nav class="main-nav vertical-division">
                    <a href="#">Presentación</a>
                    <a href="#">Requisitos</a>
                    {% block header %}
                    <!-- Espacio reservado por si en el futuro agregamos navegación -->
                    {% endblock %}
                </nav>
                <div class="support drop-menu">
                    <span>¿Te gustó el sitio?</span>
                    <div class="menu-content">
                        <a href="#">Agregar a favoritos</a>
                        <a href="#">Enviar sugerencias</a>
                        <a href="#">Reportar un error</a>
                        <a href="#">Compartir NeuroticLab</a>
                        <a href="#">Donaciones</a>
                    </div>
                </div>
            </nav>
        </header>
        <div class="site-middle">
            <aside class="modules">
                <span>Módulos</span>
                <ol start="0">
                    <li><a href="#">Matematicas</a></li>
                    <li><a href="#">Perceptron booleano</a></li>
                    <li><a href="#">Resolviendo XOR</a></li>
                </ol>
            </aside>
            <main class="content">
                <div class="inner">
                    {% block content %}
                        {{ content | safe }}
                    {% endblock %}
                </div>
            </main>
        </div>
        <footer class="horizontal-bar site-footer">
            <nav class="container">
                <p><a href="/licencia/">LICENCIA</a></p>
                <p>Lo difícil no cambió. Tú sí.</p>
                <p>Hecho por Titux</p>
                {% block footer %}
                <!-- Bloque editable para pie de página extendido -->
                {% endblock %}
            </nav>
        </footer>
    </body>
</html>